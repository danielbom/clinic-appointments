{
  "$id": "https://dev-clinic-appointments.com.br/schemas/errors/ProblemDetails.json",
  "type": "object",
  "description": "Represents a standardized error response following RFC 7807. Provides both machine-readable and human-readable information about an error condition.",
  "properties": {
    "code": {
      "type": "string",
      "description": "A machine-readable, domain-specific error code used by clients to implement conditional logic.",
      "enum": [
        "auth_error",
        "forbidden_error",
        "validation_error",
        "resource_not_found",
        "resource_conflict",
        "internal_error"
      ]
    },
    "type": {
      "type": "string",
      "format": "uri",
      "description": "A URI reference that identifies the problem type. This value resolve to documentation describing the error category and possible resolutions."
    },
    "title": {
      "type": "string",
      "description": "A short, human-readable summary of the problem type. It remain stable across occurrences of the same error."
    },
    "status": {
      "type": "number",
      "description": "The HTTP status code returned for this occurrence of the problem."
    },
    "detail": {
      "type": "string",
      "description": "A human-readable explanation specific to this occurrence of the problem, providing additional context when necessary."
    },
    "instance": {
      "type": "string",
      "description": "A URI reference that identifies the specific occurrence of the problem, such as a request path or a correlation resource."
    },
    "traceId": {
      "type": "string",
      "description": "A unique identifier used for tracing the request across distributed systems, useful for diagnostics and support."
    },
    "source": {
      "type": "object",
      "description": "Indicates the location of the input associated with the error, such as request body, path parameters, or query parameters.",
      "properties": {
        "in": {
          "type": "string",
          "enum": [
            "body",
            "path",
            "query"
          ]
        },
        "path": {
          "type": "string"
        }
      },
      "required": [
        "in",
        "path"
      ]
    },
    "errors": {
      "type": "object",
      "description": "A map of validation errors where each key is a field or parameter name and the value is a list of associated validation messages.",
      "additionalProperties": {
        "type": "array",
        "items": {
          "description": "A validation error message for the associated field.",
          "type": "string"
        }
      },
      "properties": {}
    }
  },
  "required": [
    "code",
    "type",
    "title",
    "status"
  ]
}